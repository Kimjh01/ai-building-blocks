from __future__ import annotations

from pathlib import Path


def summarize_image(image_path: str | Path) -> dict[str, str]:
    """Placeholder image utility for future expansion."""
    return {
        "image_path": str(image_path),
        "status": "placeholder",
        "message": "Implement image processing logic here.",
    }

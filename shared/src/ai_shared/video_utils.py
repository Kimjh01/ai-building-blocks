from __future__ import annotations

from pathlib import Path


def summarize_video(video_path: str | Path) -> dict[str, str]:
    """Placeholder video utility for future expansion."""
    return {
        "video_path": str(video_path),
        "status": "placeholder",
        "message": "Implement video processing logic here.",
    }
